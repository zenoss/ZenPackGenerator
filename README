# THIS CODE IS GPL2.  The appropriate headers and licenses havent been put in place yet.

# As zenoss user in zenoss 2.4+
# Development install
python setup.py develop

# Using
zpg -h

# zenpacks are placed in ZenPackGenerator/src/zpg/build/ by default
# Sample json input files can be found in the samples folder
#
# the tools folder contain scripts to aid in zenpack development
#
#
#
# Unit tests with test coverage 
nosetests --with-coverage --cover-erase

#TODO
1.) possible source template versioning so future tools can lookup the original templates.
2.) packZproperties and groups on the root init.py
3.) register devtypes setting for the snmp,ssh, or wmi device classes.... (for the add multiple devices dropdown).
4.)

